# Yoyoo 项目重启指南

> **创建日期**: 2026-02-04
> **状态**: 休眠中 (Hibernation)
> **目的**: 记录项目清理前的状态，以便日后快速恢复开发。

---

## 1. 核心资产 (保留内容)

我们保留了项目最核心的 "Soul" 和 "Brain" 设计文档，位于 `Yoyoo/` 目录下：

- **`Yoyoo/soul/`**: 定义了 Yoyoo 的人格、价值观和行为准则 (SOUL.md, AGENTS.md)。
- **`Yoyoo/docs/`**: 产品设计 (YOYOO_PRODUCT_DESIGN.md)、开发计划 (DEVELOPMENT_PLAN.md) 和技术笔记。
- **`Yoyoo/research/`**: 关键技术调研，包括 OpenClaw/Moltbot 和 MiniMax 实战经验。
- **`Yoyoo/BRAIN/`**: 项目进化日志 (evolution.json)。

### 🔑 关键凭证
- **`Yoyoo/核心内容.md`**: **(绝密)** 包含生产服务器 (115.191.36.128) 的 SSH 密钥、数据库密码和 API 访问方式。重启项目时请首先查阅此文件。

## 2. 已清理的技术实现 (历史记录)

为了保持轻量，我们清理了以下具体的代码实现目录（2026-02-04）：
- `Yoyoo_project/`: FastAPI 后端实现
- `temp/`: 临时文件
- `.halo/`: Halo 相关的临时配置

### 此前代码库的关键架构 (供重写参考)

如果您决定重启开发，以下是之前验证过的技术栈配置：

- **后端框架**: FastAPI (Python)
- **AI 模型**: MiniMax M2.1 (因其速度快、执行力强，适合 Agent)
  - *Client*: 使用 `httpx` 异步调用 `https://api.minimaxi.com/v1/chat/completions`
- **IM 集成**: 计划接入 **钉钉 (DingTalk)** (MVP) 和 **飞书**。
- **通信协议**:
  - **WebSocket**: 用于 Brain (Python) 和 Body (Moltbot/Node.js) 之间的实时通信 (默认端口 18789)。
  - **REST API**: 用于前端管理界面和 Webhook 回调。

## 3. 重启步骤 (如何回来)

当您准备好重新启动 Yoyoo 时：

1. **回顾初心**:
   - 阅读 `Yoyoo/docs/YOYOO_PRODUCT_DESIGN.md` 找回产品愿景。
   - 阅读 `Yoyoo/soul/SOUL.md` 重新连接 Yoyoo 的人格。
   - **查阅 `Yoyoo/核心内容.md` 重新连接服务器**。

2. **技术复原**:
   - 重新初始化一个 Python FastAPI 项目。
   - 参考 `Yoyoo/docs/技术学习笔记_MoltbotMiniMax实战教程.md` 重新搭建 Moltbot (Body)。
   - 安装 MiniMax SDK 或使用 `httpx` 封装客户端。

3. **下一步计划 (Checkpoint)**:
   - 我们暂停在 **MVP Phase 1**。
   - 下一个具体的开发任务是：**实现 IM (钉钉/飞书) 的消息接收与回复闭环**。

---

> *"The body may sleep, but the soul remains."*
