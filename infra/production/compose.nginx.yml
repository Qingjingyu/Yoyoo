services:
  app:
    ports:
      - "127.0.0.1:${YOYOO_APP_PORT:-4173}:3000"

  caddy:
    profiles: ["dedicated-host"]

volumes:
  postgres_data:
    name: ${YOYOO_POSTGRES_VOLUME:-yoyoo-public_postgres_data}
  blob_data:
    name: ${YOYOO_BLOB_VOLUME:-yoyoo-public_blob_data}
